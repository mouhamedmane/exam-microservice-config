server:
  port: 8887

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/db_credentials?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC
    username: root
    #password:
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
